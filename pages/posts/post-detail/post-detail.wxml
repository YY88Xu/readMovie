<!--pages/post-detail/post-detail.wxml-->
<view class="container">
<image src="{{imgSrc}}" class="head-image"></image>
<view class="author-date">
  <image src="{{avatar}}" class="avatar"></image>
  <text class="author">{{author}}</text>
  <text class="const-text">发表于</text>
  <text class="date">{{dateTime}}</text>
</view>
<text class="title">{{title}}</text>
<view class="tool">
  <view class="circle-img">
    <image src="/images/icon/collection.png" bindtap="tapCollection" wx:if="{{collected}}"></image>
    <image src="/images/icon/collection-anti.png" bindtap="tapCollection" wx:else></image>
    <image src="/images/icon/share.png" class="share-img"></image>
  </view>
  <view class="horizon"></view>
</view>
<text class="detail">{{detail}}</text>
</view>
